<%= form_for(@bike) do |f| %>
    <%= render "shared/form_errors", object: f.object %>

    <div class="field">
      <%= f.label :name %>
      <br>
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
